/**
 * NEXUS - WorkflowEditor.js (DÉPRÉCIÉ)
 *
 * Ce fichier n'est plus chargé par index.html.
 * La classe WorkflowEditor a été découpée en sous-modules dans js/WorkflowEditor/ :
 *
 *   core.js      — class WorkflowEditor + constructeur
 *   nodes.js     — gestion des nœuds, sérialisation, layout, sélection
 *   links.js     — liens, rendu SVG, correspondance paramètres, caméra
 *   builders.js  — constructeurs HTML (tous types de nœuds)
 *   python.js    — chargement script, visibilité ports
 *   events.js    — événements workspace, nœuds, variable
 *   subflow.js   — modal sous-processus (fonctions globales)
 */
